{% extends 'base.html.twig' %}

{% block title %}{{member.firstname~" "~member.lastname}}{% endblock %}

{% block body %}
    
    {% include 'user/profil.html.twig' with {profil:member} only %}

    <section style="display:flex;flex-direction:row;margin:10px;width:100%;justify-content:space-between;">
    <a href="{{ path('member_index') }}">Retour à la liste</a>
        {% if app.user and app.user.isAdmin() %}
            <a href="{{ path('member_edit', {'id': member.id}) }}">Editer</a>
        {{ include('member/_delete_form.html.twig') }}

        {% endif %}
    </section>



{% endblock %}
